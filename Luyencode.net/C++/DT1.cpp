#include <iostream>
#include <cmath>
#include <iomanip>
#define ll long long int
using namespace std;

int main()
{
    ll a;
    cin >> a;
    cout << fixed << setprecision(3) << (a*a*M_PI)/2 << endl;
    return 0;
}